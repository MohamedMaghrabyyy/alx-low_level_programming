#include <stdio.h>
/**
 * main - entry point
 * @argc: number of args
 * @argv: array of pointers pointing to each argument
 *
 * Return: nothing
 */

int main(int argc, char *argv[])
{
	printf("%d", argc);
	putchar('\n');

	return (0);
}
