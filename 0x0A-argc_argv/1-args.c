#include <stdio.h>
/**
 * main - entry point
 * @argc: number of args
 * @argv: array of pointers pointing to each argument
 *
 * Return: nothing
 */

int main(int argc, char __attribute__ ((unused)) *argv[])
{
	printf("%d", argc);
	putchar('\n');

	return (0);
}
