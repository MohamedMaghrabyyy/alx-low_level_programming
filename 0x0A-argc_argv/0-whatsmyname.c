#include <stdio.h>
/**
 * main - Entry point
 * @argc: number of arguments
 * @argv: array of pointers to arguments 
 *
 * Return: nothing
 */


int main(int argc, char *argv[])
{

	printf("%s", argv[0]);
	putchar('\n');

	return (0);
}
