#include <stdio.h>
#include "main.h"
/**
 * print_sign - checks for sign of number
 * @n: number to be checked
 *
 * Return: 1 if postive
 * 0 if zero
 * -1 if negative
 */

int print_sign(int n)
{
	int i;

	if (i > 0)
	{
		return (1);
	}
	else if (i == 0)
	{
		return (0);
	}
	else
	{
		return (-1);
	}
}
